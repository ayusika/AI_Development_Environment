# 更新履歴

## v0.0.1 (2026-07-31)

### Added

- AI Development Environment リポジトリ作成
- Git 初期化
- GitHub SSH接続
- 初回Push
- ルート README 作成
- 000_HOME README 作成
- 決定事項 作成
- VS Code 開発環境導入

---

## 2026-08-09｜Writer長文Transport・セーブ経路更新

### Writer Adaptive Chunk Transport

Koppy World WriterへAdaptive Chunk Transportを統合。

通常のProposal送信がWAFによってHTTP 403となった場合、自動的にAdaptive Chunk Transportへフォールバックする構成とした。

長文をBase64へ変換して分割し、WAFを通過できないChunkのみをさらに再分割する。

全Chunk送信後はサーバー側で再結合し、SHA-256による完全性検証を行ってからProposalを生成する。

本番Koppy WorldのWriter UIから長文Proposalを作成し、DevToolsによる手動分割・送信なしで正常完走することを確認した。

### Koppy World UI Deploy

`900_Lab/Web_KoppyOS_Beta/index.html`

を

`/koppy/index.html`

へDeployするExact Match AllowlistをDeploy系APIへ追加。

Koppy World UI自身をProposal → Approval → Execute経路で本番反映できるようにした。

### 000_HOME Writer権限

以下4ファイルをWriterのExact Match Allowlistへ追加。

- `000_HOME/決定事項.md`
- `000_HOME/現在の状態.md`
- `000_HOME/更新履歴.md`
- `000_HOME/次にやること.md`

これにより「セーブ！」で使用するKoppyOS運用情報をWriter Proposal経由でGitHubへ保存できる基盤が開通した。

---

## 2026-08-09｜UI Deploy target表示不整合 修正

Deploy ExecutorでKoppy World UIをDeployした際、実際の配置先は `/koppy/index.html` で正常であるにもかかわらず、実行レスポンスおよびDeploy Log上の配置先が `/koppy/api/` と誤表示される不整合を修正した。

### 修正内容

`600_KoppyOS/server/api/v1/deploy/executor.php` の以下2箇所を、Deploy対象に応じて分岐するよう修正。

- Deploy Logの `target_server_path`
- 実行レスポンスの `server_target`

APIファイルの場合：

`/koppy/api/...`

Koppy World UIの場合：

`/koppy/index.html`

### 本番確認

修正版Deploy Executorを本番反映し、hash / size検証の成功を確認。

Koppy World UIについても、

`server_target: "/koppy/index.html"`

が正しく返ることを確認した。

これにより、UI Deployの実配置・Deploy Log・APIレスポンスで配置先の表現が一致した。

---

## 2026-09-02｜Koppy World ファイル分割・CSS責務整理 完了

Koppy Worldの画面別ファイル分割と、
共有CSSの責務整理を実施した。

### 完了内容

- Writer専用画面分離・本番確認
- Koppy Chat専用画面分離・Runtime確認
- Brain専用画面分離・Deploy・CSS整理・本番確認
- System専用画面分離・localhost Runtime確認
- Home JavaScript cleanup
- Home CSS split
- Writer Proposal CSS split
- Home `@keyframes float` を `home.css` へ移管
- shared `style.css` の責務整理

Writer Proposal関連の専用CSSは
`assets/css/writer.css` へ移管し、
shared `style.css` からProposal / Patch専用セレクタ本体を撤去した。

Home専用CSSは
`assets/css/home.css`
へ分離し、
Home専用Animation `@keyframes float` も移管した。

共有Animationについては、

- `@keyframes drift`：Background Orb用として共有維持
- `@keyframes pulse`：Brain / Writer共用として共有維持

とした。

### 実機確認

本番：

`https://koppy.miki-piano.com/`

- Home final smoke：PASS
- Writer Proposal final smoke：PASS

localhost：

`http://localhost:8000/system/`

- System Runtime：PASS

今回のファイル分割・リファクタリング区間は
完了チェックポイントとする。

---

## 2026-09-07｜Kohaku Work 確定済み売上修正・予約再計算 完成

Kohaku Workの確定済み売上について、
確定後の訂正を安全に行うための機能を実装し、
本番実地テストまで完了した。

### 確定済み売上の手動修正

確定済み売上で以下を修正可能にした。

- チップ
- 値引き
- 値引き理由
- 値引きメモ
- 調整額

確定時のコース・OP・指名料snapshotは維持したまま、
顧客支払額と手取り合計を再計算する。

変更前後は
`visit_sales_history`
へ保存する。

### 確定済みsnapshotの固定表示

確定済み売上は、
現在の予約内容や料金マスタから再計算せず、
確定時に保存したsnapshotを表示の正本として使用するよう整理した。

これにより、
予約や料金マスタを後から変更しても
過去の確定済み売上が意図せず変化しない。

### 予約内容から売上を再計算

確定後に予約内容自体の誤りが判明した場合に備え、

`予約内容から売上を再計算`

機能を追加した。

予約側で訂正した以下の内容から
確定済み売上snapshotを再作成できる。

- コース
- 延長
- OP
- 指名料

以下は既存の確定値を維持する。

- チップ
- 値引き
- 値引き理由
- 値引きメモ
- 調整額
- confirmed_at

再計算前後の売上データは
`visit_sales_history`
へ保存する。

### Backend

主な変更：

- `koppyCalculateVisitSales()`
  - 確定済みsnapshot固定表示
  - 予約再計算用の内部モード追加

- `koppyRecalculateConfirmedVisitSales()`
  - 現在予約から新snapshotを作成
  - Transaction内で売上更新
  - 変更履歴保存
  - confirmed_at維持

- `visit-sales.php`
  - `mode: recalculate` を追加
  - `revise` と同様にTransaction経路で処理

### Frontend

確定済み売上へ以下の操作を追加。

- `売上を修正`
- `予約内容から売上を再計算`

再計算前には確認ダイアログを表示し、
成功後は最新の確定済み売上を再取得する。

### 本番確認

Kohaku Work本番環境で以下を確認。

- 確定済み売上修正：PASS
- 調整額修正後の合計更新：PASS
- 確定済み売上のsnapshot固定表示：PASS
- 予約コース訂正後の売上再計算：PASS
- 新しいコース料金への更新：PASS

### 完了判定

**確定済み売上 手動修正：DONE**

**確定済みsnapshot固定表示：DONE**

**予約内容から確定済み売上を再計算：DONE**

**本番実地テスト：PASS**

Kohaku Workの
「売上確定後の金額訂正・予約内容訂正」
基本経路を完成とする。
